@{
  Layout="Shared/_Layout.cshtml";
}
<h1>List of All Categories</h1>
@if (@Model.Count == 0)
{
  <p>No categories have been added yet!</p>
}
    @foreach (var category in Model)
    {
      <h2><a href="/category/@category.GetId()">@category.GetName()</a> | <a href="/category/edit/@category.GetId()">Edit</a> | <a href="/category/delete/@category.GetId()">Delete</a></h2>
    }
  <form action="/categories/delete" method="post">
    <button class="btn btn-lg btn-primary" type="submit">Clear All</button>
  </form>
